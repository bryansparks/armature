# Copy to a gitignored location (e.g. ~/.armature/secrets.env) and fill in.
# This file carries only a placeholder — never commit real keys.
# The package bundles secret NAMES only; values are injected at run from --profile.
ANTHROPIC_API_KEY=your-anthropic-key-here